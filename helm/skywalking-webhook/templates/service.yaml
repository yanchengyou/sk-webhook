apiVersion: v1
kind: Service
metadata:
  name: {{ include "skywalking-webhook.fullname" . }}
  labels:
    {{- include "skywalking-webhook.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "skywalking-webhook.selectorLabels" . | nindent 4 }}
